# app/# app/retriever.py
def retrieve_relevant_clauses(parsed_query):
    # Fake logic — replace with FAISS or LLM-based retriever later
    return [
        {
            "clause": "Knee surgeries are covered after a waiting period of 90 days.",
            "matched_keywords": ["knee surgery", "90 days", "waiting period"]
        }
    ]
.py
def retrieve_relevant_clauses(parsed_query):
    # Fake logic — replace with FAISS or LLM-based retriever later
    return [
        {
            "clause": "Knee surgeries are covered after a waiting period of 90 days.",
            "matched_keywords": ["knee surgery", "90 days", "waiting period"]
        }
    ]
